using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// 단두대에 줄 20개 연결 첨에 랜덤으로 단두대 떨어질 줄 0-19 중 하나 정함
/// 그거 자르면 단두대 떨어짐, 손가락 뺀 애들로 승리 판단
/// 상대1 상대2 내 손 손 세개, 단두대에 배치, 나는 버튼 누르면 손가락 빠짐 true, 상대는 매 판마다 랜덤인데
/// 판 뒤로 갈수록 확률 up
/// </summary>
public class FingerScaffoldManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
